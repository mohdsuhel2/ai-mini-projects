# Copy to .env and fill in real values. Never commit .env.

# Live trading via VPS gateway (recommended on Mac — any WiFi).
GROWW_GATEWAY_URL=https://noobius.in/groww
GROWW_GATEWAY_TOKEN=

# Direct Groww SDK (only on whitelisted static IP, e.g. the VPS — not needed on Mac).
# GROWW_API_KEY=
# GROWW_TOTP_SECRET=
