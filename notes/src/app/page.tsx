import { SimplyNotesApp } from '@/components/layout/app'

/**
 * The app, and nothing under it. The marketing prose and FAQ live at /about —
 * a tool you open daily should not make you scroll past its own pitch.
 */
export default function HomePage() {
  return <SimplyNotesApp />
}
